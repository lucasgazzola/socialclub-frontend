# Copiá este archivo como `.env` y ajustá según tu entorno.
# Vite solo expone al cliente las variables que empiezan con VITE_.

# URL base de la API del backend (incluye el prefijo /api/v1).
VITE_API_URL=http://localhost:3000/api/v1
