# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Environment
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.tsbuildinfo

# Documento de credenciales (secretos reales) — NUNCA commitear
CREDENCIALES.md

# Reporte de cobertura (generado)
coverage/
# Documentación binaria del proyecto (vive en Google Drive / repo backend)
docs/
# Config local de Claude Code (por usuario)
.claude/settings.local.json

# Artefactos generados por los skills de testing (evidencia bajo demanda)
jest-results.json
vitest-results.json
registro-ejecucion*.csv
